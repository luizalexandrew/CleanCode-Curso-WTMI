//G5: Duplicação

function x() {
	...

	var options = {weekday: "long", year: "numeric", month: "long", day: "numeric", hour: '2-digit', minute:'2-digit'};
	let data =  new Date(timestamp).toLocaleString('pt-BR', options);

	...
}



function y() {
	...

	var options = {weekday: "long", year: "numeric", month: "long", day: "numeric", hour: '2-digit', minute:'2-digit'};
	let data =  new Date(timestamp).toLocaleString('pt-BR', options);

	...
}


function z() {
	...

	var options = {weekday: "long", year: "numeric", month: "long", day: "numeric", hour: '2-digit', minute:'2-digit'};
	let data =  new Date(timestamp).toLocaleString('pt-BR', options);

	...
}


